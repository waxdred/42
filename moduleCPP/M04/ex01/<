#ifndef DOG_HPP
# define DOG_HPP

#include "Animal.hpp"
#include "Brain.hpp"
# include <iostream>

class	Dog : private Brain, public Animal{
	private:	
		std::string _type;
	public:
		Dog(void);
		Dog(const Dog &Dog);
		~Dog(void);

		Dog &operator = (const Dog &dog);
		void	makeSound(void);
		std::string	getType(void)const;
};
#endif
